name: Diagnostico Ultimo Resumo Diario
on:
  workflow_dispatch: {}
jobs:
  d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s -w "\nSTATUS:%{http_code}\n" --max-time 20 "https://api.168.138.232.237.sslip.io/resumos?tipo=diario&limit=1" -o backups/diag-ultimo-resumo.txt
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/diag-ultimo-resumo.txt
          git commit -m "diag ultimo resumo [skip ci]" || echo "nothing"
          git fetch origin main
          git reset --soft origin/main
          git add backups/diag-ultimo-resumo.txt
          git commit -m "diag ultimo resumo [skip ci]" || echo "nothing2"
          git push origin HEAD:main
