name: Enviar Comunicados Real
on:
  workflow_dispatch: {}
jobs:
  e:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - env:
          WEBHOOK_SECRET: ${{ secrets.FALHAS_WEBHOOK_SECRET }}
        run: |
          mkdir -p backups
          curl -s "https://whatsapp-painel-falhas.onrender.com/enviar-comunicados-diarios" -H "X-Webhook-Secret: $WEBHOOK_SECRET" -o backups/envio-real-result.json
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/envio-real-result.json
          git diff --quiet --cached || git commit -m "envio real comunicados [skip ci]"
          git push
