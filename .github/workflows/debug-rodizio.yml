name: Debug rodizio e OS 9104-9107

on:
  workflow_dispatch: {}

jobs:
  debug:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Chamar atualizar-os-agora de novo
        env:
          WEBHOOK_SECRET: ${{ secrets.FALHAS_WEBHOOK_SECRET }}
        run: |
          BASE="https://whatsapp-painel-falhas.onrender.com"
          mkdir -p backups
          curl -s -X POST --max-time 100 "$BASE/atualizar-os-agora" -o /tmp/r1.json
          sleep 5
          curl -s -X POST --max-time 100 "$BASE/atualizar-os-agora" -o /tmp/r2.json
          curl -s "$BASE/atividades" -H "X-Webhook-Secret: $WEBHOOK_SECRET" -o /tmp/a.json
          python3 -c "
          import json
          d = json.load(open('/tmp/a.json'))
          for a in d.get('atividades', []):
              if a.get('numeroOS') == '9107':
                  print(a.get('numeroOS'), '| status=', a.get('status'), '| statusOS=', a.get('statusOS'), '| pct=', a.get('percentualOS'))
          " > backups/check-status-result.txt
          cat backups/check-status-result.txt
      - name: Salvar resultado
        if: always()
        run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/check-status-result.txt
          git diff --quiet --cached || git commit -m "Debug rodizio [skip ci]"
          git push
