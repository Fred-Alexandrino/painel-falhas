name: Teste PWA
on:
  workflow_dispatch: {}
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "=== manifest.json ===" 
          curl -s -o /dev/null -w "HTTP:%{http_code} TYPE:%{content_type}\n" "https://fred-alexandrino.github.io/PAINELDEFALHAS/manifest.json"
          curl -s "https://fred-alexandrino.github.io/PAINELDEFALHAS/manifest.json"
          echo ""
          echo "=== icon-192.png ==="
          curl -s -o /dev/null -w "HTTP:%{http_code} TYPE:%{content_type} SIZE:%{size_download}\n" "https://fred-alexandrino.github.io/PAINELDEFALHAS/icon-192.png"
          echo "=== icon-512.png ==="
          curl -s -o /dev/null -w "HTTP:%{http_code} TYPE:%{content_type} SIZE:%{size_download}\n" "https://fred-alexandrino.github.io/PAINELDEFALHAS/icon-512.png"
