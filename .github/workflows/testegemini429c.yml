name: Teste Gemini 429c
on:
  workflow_dispatch: {}
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -s -m 40 -X POST "https://api.168.138.232.237.sslip.io/gerar-comunicado-livre-ia" -H "Content-Type: application/json" -d '{"tema":"Teste de resiliencia","observacoes":"apenas um teste"}' -w "\nHTTP_CODE:%{http_code}\n" | tee /tmp/resultado.txt
          echo "::notice::$(cat /tmp/resultado.txt)"
