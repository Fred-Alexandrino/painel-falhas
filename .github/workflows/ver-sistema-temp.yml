name: Config Set Lote Clusters
on:
  workflow_dispatch: {}
jobs:
  d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          echo "=== antes ===" > backups/config-clusters.txt
          curl -s "https://api.168.138.232.237.sslip.io/config-ler" -H "X-Webhook-Secret: falhas2026" >> backups/config-clusters.txt
          echo "" >> backups/config-clusters.txt
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/config-clusters.txt
          git commit -m "config clusters antes [skip ci]" || echo "nothing"
          git fetch origin main
          git reset --soft origin/main
          git add backups/config-clusters.txt
          git commit -m "config clusters antes [skip ci]" || echo "nothing2"
          git push origin HEAD:main
