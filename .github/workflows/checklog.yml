name: Check Log Mensagens
on:
  workflow_dispatch: {}
jobs:
  c:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - env:
          WEBHOOK_SECRET: ${{ secrets.FALHAS_WEBHOOK_SECRET }}
        run: |
          mkdir -p backups
          curl -s -m 50 -X POST "https://api.168.138.232.237.sslip.io/gerar-resumo-diario?data=2026-07-28" -H "X-Webhook-Secret: $WEBHOOK_SECRET" -o backups/envio-ontem.json
          cat backups/envio-ontem.json
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/envio-ontem.json
          git diff --quiet --cached || git commit -m "envio ontem [skip ci]"
          git push
