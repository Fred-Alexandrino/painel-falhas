name: Check Log Mensagens
on:
  workflow_dispatch: {}
jobs:
  c:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s -m 20 "https://api.168.138.232.237.sslip.io/mensagens-grupo-diagnostico?dias=3" -o backups/mensagens-diag.json
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/mensagens-diag.json
          git diff --quiet --cached || git commit -m "mensagens diag [skip ci]"
          git push
