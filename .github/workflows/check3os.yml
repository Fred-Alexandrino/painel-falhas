name: Check 3 OS Araputanga
on:
  workflow_dispatch: {}
jobs:
  c:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s "https://whatsapp-painel-falhas.onrender.com/atividades" -o /tmp/a.json
          python3 -c "
          import json
          d = json.load(open('/tmp/a.json'))
          ativs = d.get('atividades', [])
          for a in ativs:
              if a.get('numeroOS') in ('9248','9284','9300'):
                  print(json.dumps(a, ensure_ascii=False, indent=2))
                  print('---')
          " > backups/check3os.txt
          cat backups/check3os.txt
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/check3os.txt
          git diff --quiet --cached || git commit -m "check 3 os [skip ci]"
          git push
