name: Verificar OS 9359 (temp)
on:
  workflow_dispatch: {}
jobs:
  verificar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s -X POST "https://api.168.138.232.237.sslip.io/verificar-uma-os" \
            -H "Content-Type: application/json" \
            -d '{"numeroOS":"9359"}' -o backups/verificar-9359.json
      - run: |
          git config user.name "diag-bot"
          git config user.email "actions@github.com"
          git add backups/verificar-9359.json
          git diff --quiet --cached || git commit -m "verificar OS 9359 [skip ci]"
          git push
