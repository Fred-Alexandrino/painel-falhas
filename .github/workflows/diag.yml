name: Diagnostico Sync
on:
  workflow_dispatch: {}
jobs:
  d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s -w "\nSTATUS:%{http_code}\n" --max-time 15 "https://api.168.138.232.237.sslip.io/health" -o backups/diag-health.txt
          curl -s -w "\nSTATUS:%{http_code}\n" --max-time 30 "https://api.168.138.232.237.sslip.io/programacao-pcm" -o backups/diag-programacao-pcm.txt
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/diag-health.txt backups/diag-programacao-pcm.txt
          git commit -m "diag sync [skip ci]" || echo "nothing"
          git fetch origin main
          git reset --soft origin/main
          git add backups/diag-health.txt backups/diag-programacao-pcm.txt
          git commit -m "diag sync [skip ci]" || echo "nothing2"
          git push origin HEAD:main
