name: Diagnostico Sync
on:
  workflow_dispatch: {}
jobs:
  d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - env:
          WEBHOOK_SECRET: ${{ secrets.FALHAS_WEBHOOK_SECRET }}
        run: |
          mkdir -p backups
          curl -s -w "\nHTTP_STATUS:%{http_code}\n" "https://api.168.138.232.237.sslip.io/health" -o backups/diag-v-health.json
          curl -s -w "\nHTTP_STATUS:%{http_code}\n" "https://api.168.138.232.237.sslip.io/zeladoria-fotos-status" -o backups/diag-v-status.json
          curl -s -w "\nHTTP_STATUS:%{http_code}\n" "https://api.168.138.232.237.sslip.io/zeladoria-graus" -o backups/diag-v-graus.json
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/diag-v-health.json backups/diag-v-status.json backups/diag-v-graus.json
          git diff --quiet --cached || git commit -m "diag sync [skip ci]"
          git push
