name: Check Status Conexao Atual
on:
  workflow_dispatch: {}
jobs:
  c:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s "https://wppconnect-painel.onrender.com/health" --max-time 20 -o backups/status-atual.json
          echo "" >> backups/status-atual.json
          curl -s "https://wppconnect-painel.onrender.com/qr" --max-time 20 >> backups/status-atual.json
          cat backups/status-atual.json
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/status-atual.json
          git diff --quiet --cached || git commit -m "check status atual [skip ci]"
          git push
