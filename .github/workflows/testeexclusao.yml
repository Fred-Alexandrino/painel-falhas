name: Teste Exclusao Finalizada
on:
  workflow_dispatch: {}
jobs:
  t:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - env:
          WEBHOOK_SECRET: ${{ secrets.FALHAS_WEBHOOK_SECRET }}
        run: |
          mkdir -p backups
          curl -s -X POST "https://whatsapp-painel-falhas.onrender.com/enviar-comunicados-diarios?dry_run=true" -H "X-Webhook-Secret: $WEBHOOK_SECRET" --max-time 60 -o backups/teste-exclusao.json
          python3 -c "
          import json
          d = json.load(open('backups/teste-exclusao.json'))
          print('ok:', d.get('ok'))
          print('enviados:', len(d.get('enviados',[])))
          print('erros:', d.get('erros'))
          " > backups/resumo-exclusao.txt
          cat backups/resumo-exclusao.txt
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/resumo-exclusao.txt
          git diff --quiet --cached || git commit -m "teste exclusao [skip ci]"
          git push
