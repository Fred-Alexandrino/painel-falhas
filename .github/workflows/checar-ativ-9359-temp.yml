name: Checar Atividade 9359 (temp)
on:
  workflow_dispatch: {}
jobs:
  checar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          curl -s "https://api.168.138.232.237.sslip.io/atividades" -o backups/ativ_9359_check.json
      - run: |
          git config user.name "diag-bot"
          git config user.email "actions@github.com"
          git add backups/ativ_9359_check.json
          git diff --quiet --cached || git commit -m "check ativ 9359 [skip ci]"
          git push
