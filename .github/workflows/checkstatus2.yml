name: Check Status Pos Deploy
on:
  workflow_dispatch: {}
jobs:
  c:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s "https://wppconnect-painel.onrender.com/health" --max-time 20 -o backups/status-pos-deploy.json
          cat backups/status-pos-deploy.json
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/status-pos-deploy.json
          git diff --quiet --cached || git commit -m "check status pos deploy [skip ci]"
          git push
