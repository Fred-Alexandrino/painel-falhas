name: Fix Matao Cluster Cleanup
on:
  workflow_dispatch: {}
jobs:
  d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Remover chaves erradas (Matao I romano) e limpar cache
        env:
          WEBHOOK_SECRET: ${{ secrets.FALHAS_WEBHOOK_SECRET }}
        run: |
          mkdir -p backups
          curl -s -X POST "https://api.168.138.232.237.sslip.io/config-remover" \
            -H "X-Webhook-Secret: $WEBHOOK_SECRET" -H "Content-Type: application/json" \
            -d '{"chaves":["cluster_usina:Matão I","grupo_usina:Matão I"]}' -o backups/diag-remover-result.json
          curl -s -X POST "https://api.168.138.232.237.sslip.io/config-limpar-cache" \
            -H "X-Webhook-Secret: $WEBHOOK_SECRET" -o backups/diag-limpar-cache-result.json
          curl -s -H "X-Webhook-Secret: $WEBHOOK_SECRET" "https://api.168.138.232.237.sslip.io/config-ler" -o backups/diag-config-ler-pos.json
      - if: always()
        run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/diag-remover-result.json backups/diag-limpar-cache-result.json backups/diag-config-ler-pos.json
          git diff --quiet --cached || git commit -m "cleanup matao cluster keys temp"
          git fetch origin main
          git reset --soft origin/main
          git add backups/diag-remover-result.json backups/diag-limpar-cache-result.json backups/diag-config-ler-pos.json
          git commit -m "cleanup matao cluster keys temp" || echo nothing
          git push origin HEAD:main
