name: Teste Resumo V2
on:
  workflow_dispatch: {}
jobs:
  t:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s "https://api.168.138.232.237.sslip.io/resumo" -o backups/teste_resumo2.json
      - run: |
          git config user.name "diag-bot"
          git config user.email "actions@github.com"
          git add backups/teste_resumo2.json
          git diff --quiet --cached || git commit -m "teste v2 [skip ci]"
          git push
