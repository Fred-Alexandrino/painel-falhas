name: Checar Hortina Vitesse
on:
  workflow_dispatch: {}
jobs:
  d:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          mkdir -p backups
          curl -s "https://api.168.138.232.237.sslip.io/config-ler" -H "X-Webhook-Secret: falhas2026" > backups/check-hv.txt
      - run: |
          git config user.name "sync-fracttal-bot"
          git config user.email "actions@github.com"
          git add backups/check-hv.txt
          git commit -m "checar hortina vitesse [skip ci]" || echo "nothing"
          git fetch origin main
          git reset --soft origin/main
          git add backups/check-hv.txt
          git commit -m "checar hortina vitesse [skip ci]" || echo "nothing2"
          git push origin HEAD:main
